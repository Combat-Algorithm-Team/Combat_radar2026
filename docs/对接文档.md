# 对接文档（Radar Algorithm Integration Guide）

## 1. 项目背景与目标
- **来源**：HKUST RoboMaster 雷达算法开源工程（https://github.com/hkustenterprize/RM2025-Radar-Algorithm）
- **问题所在**：跟踪与消息定义等模块，依赖复杂，需要统一的开发环境。
- **运行环境**：NUC（Ubuntu 22.04）中运行 Docker 容器，容纳 ROS2 Humble 工作区。
- **标准化目标**：采用 "NUC + Docker" 模式，全程在NUC上开发，实现一次开发、随处部署、快速交接。

## 2. 架构（开发/部署分工）
```
NUC (Ubuntu 22.04)         | Docker (ROS2 Humble)         |雷达算法工作区             传感器驱动（相机/串口）     

```

## 3. 设备与环境策略
### 3.1 NUC（主开发+最终部署）
- 系统：Ubuntu 22.04。
- 作用：运行 Docker 容器、映射相机设备、当作生产环境。宿主机不安装 ROS，保持干净，所有依赖由 Docker 管理。

### 3.3 Docker 容器（统一开发环境）
- 用途：ROS2 Humble、编译工具、消息依赖、调试工具。
- 保持：`Dockerfile`、`docker-compose.yml` 收入仓库，方便同步更新。
- 要点：挂载固定路径（如 `~/rm_radar_ws:/workspace`），便于 VSCode 与 colcon。

### 3.4 当前镜像（`radar-dev:cpu`）

#### 镜像构建
```bash
# 支持代理的构建方式（推荐）
docker build \
  --build-arg HTTP_PROXY=http://172.17.0.1:7890 \
  --build-arg HTTPS_PROXY=http://172.17.0.1:7890 \
  -f dockerfile -t radar-dev:cpu .

# 不需要代理的构建方式
docker build -f dockerfile -t radar-dev:cpu .
```

#### 核心组件
- **基础系统**：`ubuntu:22.04`，root 用户，方便直接访问 USB 设备
- **ROS 2**：Humble Desktop（完整开发工具链 + colcon）
- **Python 生态**：3.10 + PyTorch 2.2.2 (CPU) + Ultralytics + OpenCV + Pillow
- **数学库**：Eigen / FFTW / Armadillo / Ceres / PCL
- **系统工具**：ffmpeg、git、build-essential、vim、tmux 等开发必需工具
- **海康 MVS SDK**：通过宿主机挂载（不打包到镜像）

#### 功能范围
- ✅ **支持**：相机识别与追踪节点、ROS2 节点开发、Python 模型推理、海康工业相机驱动
- ✅ **支持**：Intel GPU 加速（OpenVINO）、PyQt5 GUI 界面
- ❌ **不支持**：激光雷达驱动、裁判系统驱动、RMOSS 专用模块、NVIDIA TensorRT
  > 若需扩展功能，在此镜像基础上追加新 RUN layer，但请勿删除相机依赖

**挂载海康相机命令**（见下方容器启动示例）：
```bash
-v /opt/MVS:/opt/MVS:ro  # 只读挂载，保护宿主机SDK
```

#### 容器启动（常用命令）
```bash

```bash
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/ttyUSB0 \
    -v /opt/MVS:/opt/MVS:ro \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

##### 📷 USB 摄像头启动（备选）
```bash
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

##### 🖥️ 支持 X11 GUI 转发（用于 rviz2 可视化）
```bash
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/ttyUSB0 \
    -e DISPLAY=$DISPLAY \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v /opt/MVS:/opt/MVS:ro \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

##### 🔧 多设备启动示例（2个摄像头 + 多个串口）
```bash
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/video1 \
    --device=/dev/ttyUSB0 \
    --device=/dev/ttyUSB1 \
    -v /opt/MVS:/opt/MVS:ro \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

**参数说明**：
- `--net=host`：使用宿主机网络，便于 ROS2 多机通信
- `--device=/dev/video*`：挂载摄像头设备
- `--device=/dev/ttyUSB*`：挂载串口设备（裁判系统等）
- `-v /opt/MVS:/opt/MVS:ro`：只读挂载宿主机 MVS SDK
- `-v ~/rm_radar_ws:/workspace`：挂载工作区，代码编辑同步到宿主机
- `-e DISPLAY`：X11 转发（可选，需要宿主机支持 X11）

## 4. 开发—调试—部署 SOP（稳定流程）
### Step 0：准备 NUC
```bash
sudo apt update
sudo apt install -y docker.io docker-compose git
sudo usermod -aG docker $USER
docker run hello-world  # 验证 Docker
```

### Step 1：克隆仓库
```bash
git clone https://github.com/hkustenterprize/RM2025-Radar-Algorithm.git
cd RM2025-Radar-Algorithm
```

### Step 2：使用 Docker 镜像

#### 方式 A：拉取已构建的镜像（推荐新人）
如果镜像已发布到私有仓库：
```bash
docker pull registry/radar-dev:cpu
```

#### 方式 B：本地构建镜像（开发/修改依赖时）
```bash
# 不需要代理
docker build -f dockerfile -t radar-dev:cpu .

# 需要代理（在国内网络下推荐）
docker build \
  --build-arg HTTP_PROXY=http://172.17.0.1:7890 \
  --build-arg HTTPS_PROXY=http://172.17.0.1:7890 \
  -f dockerfile -t radar-dev:cpu .
```

**代理配置说明**：
- 如果使用 Clash/V2Ray 代理，确保宿主机代理服务在 `7890` 端口监听
- Docker 内的 `172.17.0.1` 指向宿主机 IP，根据网络配置可能需要调整
- 无代理环境下，直接运行构建命令即可

#### 启动容器
```bash
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/ttyUSB0 \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

> 如需调整摄像头/串口数量或启用 GUI，参考**第 3.4 节容器启动示例**。

### Step 3：容器内开发与编译
```bash
cd /workspace
colcon build
source install/setup.bash
```
- 在容器内直接编辑代码、调试、运行测试节点。

### Step 4：调试相机节点
```bash
ros2 launch radar camera.launch.py  # 假设有相机专用launch
ros2 topic echo /camera/image_raw
rviz2
```
- 如需 GUI，可配置 `DISPLAY` 与 X11 转发。

### Step 6：文档与版本管理
- 增加模块时及时补 README。
- 每次调试成功更新 `CHANGELOG.md`。
- 消息定义变动后同步 `msg.md`。
- 所有文档集中在 `docs/`。

### Step 7：生产部署
```bash
docker pull registry/radar:v1.0
docker-compose up
```
- 生产环境始终固定镜像 tag，不直接使用开发版本。

## 5. 文档规范（推荐目录）
```
docs/
├── environment.md   # 环境搭建流程
├── docker.md        # 容器构建与依赖
├── architecture.md  # 系统架构与节点关系
├── launch.md        # 启动与 launch 说明
├── debug.md         # 常见调试策略（topic / rviz）
├── msgs.md          # 消息定义字段
├── contribute.md    # 新人开发规范
├── changelog.md     # 版本更新记录
└── todo.md          # 下一阶段计划
```
- 每份文档标明作者、更新时间与关联模块，便于未来追踪与交接。

## 6. 新人交接指南
确保下一位同学掌握：
1. 能启动项目（`docker-compose up`）并验证 ROS2 话题。
2. 理解各个节点职责与消息流。
3. 能重新构建镜像/容器，保证环境可复现。
4. 能在 `docs/` 下迅速定位所需资料。

## 7. 当前计划（可同步到 TDL）
1. 在 NUC 上搭建 Docker 环境（安装、创建 `radar_dev`、挂载 workspace）。
2. 编写 `docs/environment.md`，记录环境搭建流程。
3. 创建私有 Git 仓库并将工作空间推送过去。
4. 熟悉 ROS2 节点与消息流，确认各 topic/source 描述。
5. 准备 `Dockerfile` 与 `docker-compose.yml`，确保重建环境所需材料齐全。

完成以上步骤后，基础建设就绪，能安全推进算法开发。

## 8. 当前调试重点：相机识别+追踪

### 8.1 Docker 构建常见问题排查

#### 问题 1：构建超时或网络慢
**症状**：
```
E: Failed to fetch ... Connection timeout
```

**解决方案**：
```bash
# 使用代理加速（如 Clash/V2Ray）
docker build \
  --build-arg HTTP_PROXY=http://172.17.0.1:7890 \
  --build-arg HTTPS_PROXY=http://172.17.0.1:7890 \
  -f dockerfile -t radar-dev:cpu .

# 或更换 Ubuntu 镜像源（在 Dockerfile 中配置阿里源）
```

#### 问题 2：容器启动失败 - 设备不存在
**症状**：
```
Error: device ... does not exist
```

**解决方案**：
```bash
# 查看实际连接的设备
ls /dev/video* /dev/ttyUSB* /dev/ttyCH341USB*

# 根据实际设备调整启动命令
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/ttyUSB0 \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu
```

#### 问题 4：容器内无法访问宿主机文件
**症状**：容器内 `/workspace` 目录为空

**解决方案**：
```bash
# 确保宿主机目录存在
mkdir -p ~/rm_radar_ws

# 检查挂载是否成功
docker run -it --name test \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu \
    ls -la /workspace
```bash
# 确保宿主机目录存在
mkdir -p ~/rm_radar_ws

# 检查挂载是否成功
docker run -it --name test \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu \
    ls -la /workspace
```

#### 问题 5：ROS2 节点无法通信
**症状**：`ros2 node list` 看不到其他节点

**原因**：Docker 使用了自己的网络命名空间

**解决方案**：使用 `--net=host` 让容器共享宿主机网络
```bash
docker run -it --net=host -v ~/rm_radar_ws:/workspace radar-dev:cpu
```

### 8.3 针对海康相机 + Intel GPU 的完整快速启动指南

#### 首次启动流程（5分钟）

```bash
# Step 1: 构建镜像（带代理，首次约 5-10 分钟）
docker build \
  --build-arg HTTP_PROXY=http://172.17.0.1:7890 \
  --build-arg HTTPS_PROXY=http://172.17.0.1:7890 \
  -f dockerfile -t radar-dev:cpu .

# Step 2: 启动容器（海康相机专用）
docker run -it --name radar_dev \
    --net=host \
    --device=/dev/video0 \
    --device=/dev/ttyUSB0 \
    -v /opt/MVS:/opt/MVS:ro \
    -v ~/rm_radar_ws:/workspace \
    radar-dev:cpu

# Step 3: 容器内验证
cd /workspace
python -m driver.hik_camera.mock_hik  # 测试相机驱动

# Step 4: 运行识别推理
python main.py --config config/params.yaml --device_config config/device.yaml
```

#### 日常快速启动

```bash
# 已有容器直接进入
docker exec -it radar_dev bash

# 或重启容器
docker start radar_dev
docker exec -it radar_dev bash
```

#### 停止和清理

```bash
# 停止容器（保留数据）
docker stop radar_dev

# 删除容器
docker rm radar_dev

# 完全清理（包括镜像）
docker rmi radar-dev:cpu
```

### 8.4 为尽快恢复相机核心功能，近期目标聚焦在「相机输入 → 识别 → 追踪」链路：

1. **相机驱动连通性**
    - 在宿主机确认海康/USB 摄像头被识别（`v4l2-ctl --list-devices`）。
    - 容器内使用 `python -m driver.hik_camera.mock_hik` 或 `ros2 run` 对应节点验证画面。
2. **识别模型验证**
    - 利用 `model/yolov12/` 推理脚本或 `run_scripts/run.sh` 以录像/实时流测试装甲板识别。
    - 若需只跑识别，可在配置中关闭其他模块，关注 FPS 与检测框稳定度。
3. **追踪验证**
    - 启动 `tracker/CascadeMatchTracker` 相关节点，确认追踪输出是否持续。
    - 在 `ros2 topic echo /tracks` 观察目标坐标；必要时使用 `rviz2` 叠加可视化。
4. **记录 & 文档**
    - 每次调试完成在 `docs/debug.md`/`CHANGELOG.md` 补充实验环境、问题、结论。

后续若识别/追踪链路稳定，再扩展到其他模块。